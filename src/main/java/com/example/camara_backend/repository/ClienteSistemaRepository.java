package com.example.camara_backend.repository;

import com.example.camara_backend.models.ClienteSistema;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ClienteSistemaRepository extends JpaRepository<ClienteSistema, Integer> {
}
